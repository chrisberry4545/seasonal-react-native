const {
  settings
} = require('./../styles/settings');

const cHeadings = {
  cHeadingLarge: {
    fontSize: 30,
    lineHeight: 40,
    fontFamily: settings.fonts.primary
  },
  cHeadingMed: {
    color: settings.colors.primaryText,
    fontSize: 20,
    lineHeight: 40,
    fontFamily: settings.fonts.primary
  }
};

export {
  cHeadings
};

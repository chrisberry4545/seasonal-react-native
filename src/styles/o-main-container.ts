import {
  settings
} from './settings';
import { ViewStyle } from 'react-native';

export const oMainContainer: ViewStyle = {
  padding: 20,
  flex: 1,
  backgroundColor: settings.colors.backgroundColor
};

const {
  settings
} = require('./settings');

const oMainContainer = {
  oMainContainer: {
    padding: 20,
    flex: 1,
    backgroundColor: settings.colors.backgroundColor,
  }
};

module.exports = {
  oMainContainer
};
